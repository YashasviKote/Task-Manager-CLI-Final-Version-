# Task-Manager-CLI-Final-Version-
A command-line based Task Manager application built using Java.   This project demonstrates a complete backend-style system with CRUD operations, file persistence, input validation, and clean architecture.
